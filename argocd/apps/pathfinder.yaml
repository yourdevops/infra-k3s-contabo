apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pathfinder
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  source:
    repoURL: https://github.com/yourdevops/pathfinder.git
    targetRevision: main
    path: chart
    helm:
      valuesObject:
        image:
          repository: ghcr.io/yourdevops/pathfinder
          tag: latest # TODO: KRO for feature branches or just a dev env
          pullPolicy: Always
        config:
          allowedHosts: "pathfinder.yourdevops.me"
          csrfTrustedOrigins: "https://pathfinder.yourdevops.me"
        ingress:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: pathfinder
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
